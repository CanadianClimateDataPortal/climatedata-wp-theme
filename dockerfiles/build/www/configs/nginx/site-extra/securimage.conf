# Restrict access to some directories of securimage.

location /assets/themes/fw-child/resources/php/securimage/audio {
    deny all;
}

location /assets/themes/fw-child/resources/php/securimage/database {
    deny all;
}
