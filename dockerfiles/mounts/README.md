# Suggested mount directory

This directory can be used to store files and directories that are to be mounted
when building a Docker image, or when running a container. Its content is
ignored by Git.
